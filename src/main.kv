#:import HoverText main

MDBoxLayout:
    orientation: "vertical"

    MDToolbar:
        adaptive_size: True
        title: "Заметки"
        right_action_items: [["dots-vertical", lambda x: app.callback_1()], ["clock", lambda x: app.callback_2()]]

    ScrollView:
        MDList:
            id: container
            HoverText:
                text: "66768668678"
